import { OwnSurveyPipe } from './own-survey.pipe';

describe('OwnSurveyPipe', () => {
  it('create an instance', () => {
    const pipe = new OwnSurveyPipe();
    expect(pipe).toBeTruthy();
  });
});
